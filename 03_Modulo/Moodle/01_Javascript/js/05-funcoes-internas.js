/* let nome = "Ana Clara C Amorim" // Mostra até o indice indicado
console.log(nome.substring(0, 10));

let partes = nome.split(' '); // Fatia em "partes"
console.log(partes)

partes.map(function(p){ // Exibe cada um elemento por vez
    console.log(p.toUpperCase()) // O to upper case converte tudo para maiúsculo assim como o toLowerCase converte para minúsculo
}) */

// Funções matemáticas

/* let pi = -3.1416
console.log(Math.round(pi)) // Aredonda o número
console.log(Math.abs(pi)) // Ignora o sinal
console.log(Math.floor(pi)) // Número mais perto 
console.log(Math.pow(5, 2)) // Eleva um número ao expoente   */


